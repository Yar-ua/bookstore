%li.dropdown
  %a.dropdown-toggle{href: "#", 'data-toggle': "dropdown", role: "button", 'aria-haspopup': "true", 'aria-expanded': "false"}= current_user.email
  %ul.dropdown-menu
    %li
      = link_to t('header.settings'), new_setting_path
    %li
      = link_to t('header.orders'), orders_path
    %li.divider{role: "separator"}
    %li
      = link_to t('header.logout'), destroy_user_session_path, method: :delete
