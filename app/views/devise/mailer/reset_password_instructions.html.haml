%p= t('mailer.hello', email: @resource.email)
%p= t('mailer.password_change_request')
%p= link_to t('mailer.change_my_password'), edit_password_url(@resource, reset_password_token: @token)
%p= t('mailer.didnt_request')
